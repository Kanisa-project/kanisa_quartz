The `help` command is the more complicated beginner command. It accepts one of two different optional arguments, `(GROUP_TAG)` or `(COMMAND_NAME)`. 


## GROUP_TAG
Each [[TexHelpers|helper]] is part of a group, and each group has a 


## COMMAND_NAME
Using the `(COMMAND_NAME)` argument gives you full details of the named command.